"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.UsersResource = void 0;
const admin_1 = require("../../pinecone-generated-ts-fetch/admin");
const errors_1 = require("../../errors");
/**
 * Operations for managing users within the organization. Accessed via {@link AdminClient.users}.
 */
class UsersResource {
    _api;
    constructor(api) {
        this._api = api;
    }
    /** Get a user's details by ID. */
    async describe(userId) {
        if (!userId) {
            throw new errors_1.PineconeArgumentError('You must pass a non-empty string for `userId` in order to describe a user.');
        }
        return await this._api.fetchUser({
            xPineconeApiVersion: admin_1.X_PINECONE_API_VERSION,
            userId,
        });
    }
    /** List users in the organization, optionally filtered by email. */
    async list(options = {}) {
        return await this._api.listUsers({
            ...options,
            xPineconeApiVersion: admin_1.X_PINECONE_API_VERSION,
        });
    }
    /** Delete a user by ID, removing them from the organization. */
    async delete(userId) {
        if (!userId) {
            throw new errors_1.PineconeArgumentError('You must pass a non-empty string for `userId` in order to delete a user.');
        }
        return await this._api.deleteUser({
            xPineconeApiVersion: admin_1.X_PINECONE_API_VERSION,
            userId,
        });
    }
}
exports.UsersResource = UsersResource;
//# sourceMappingURL=users.js.map
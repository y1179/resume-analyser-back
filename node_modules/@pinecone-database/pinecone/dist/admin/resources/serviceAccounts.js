"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.ServiceAccountsResource = void 0;
const admin_1 = require("../../pinecone-generated-ts-fetch/admin");
const errors_1 = require("../../errors");
/**
 * Operations for managing service accounts within the organization. Accessed via
 * {@link AdminClient.serviceAccounts}.
 */
class ServiceAccountsResource {
    _api;
    constructor(api) {
        this._api = api;
    }
    /**
     * Create a new service account. The returned {@link ServiceAccountWithSecret} contains the OAuth
     * client secret, which is returned only once and cannot be retrieved later.
     */
    async create(options) {
        if (!options || !options.name) {
            throw new errors_1.PineconeArgumentError('You must pass a non-empty string for `name` in order to create a service account.');
        }
        return await this._api.createServiceAccount({
            xPineconeApiVersion: admin_1.X_PINECONE_API_VERSION,
            createServiceAccountRequest: options,
        });
    }
    /** Get a service account's details by ID. */
    async describe(serviceAccountId) {
        if (!serviceAccountId) {
            throw new errors_1.PineconeArgumentError('You must pass a non-empty string for `serviceAccountId` in order to describe a service account.');
        }
        return await this._api.fetchServiceAccount({
            xPineconeApiVersion: admin_1.X_PINECONE_API_VERSION,
            serviceAccountId,
        });
    }
    /** List all service accounts within the organization. */
    async list(options = {}) {
        return await this._api.listServiceAccounts({
            ...options,
            xPineconeApiVersion: admin_1.X_PINECONE_API_VERSION,
        });
    }
    /** Update an existing service account by ID. */
    async update(serviceAccountId, options) {
        if (!serviceAccountId) {
            throw new errors_1.PineconeArgumentError('You must pass a non-empty string for `serviceAccountId` in order to update a service account.');
        }
        return await this._api.updateServiceAccount({
            xPineconeApiVersion: admin_1.X_PINECONE_API_VERSION,
            serviceAccountId,
            updateServiceAccountRequest: options ?? {},
        });
    }
    /**
     * Rotate the OAuth client secret for a service account by ID. The returned
     * {@link ServiceAccountWithSecret} contains the new secret, which is returned only once; the
     * previous secret is invalidated.
     */
    async rotateSecret(serviceAccountId) {
        if (!serviceAccountId) {
            throw new errors_1.PineconeArgumentError('You must pass a non-empty string for `serviceAccountId` in order to rotate its secret.');
        }
        return await this._api.rotateServiceAccountSecret({
            xPineconeApiVersion: admin_1.X_PINECONE_API_VERSION,
            serviceAccountId,
        });
    }
    /** Delete a service account by ID. */
    async delete(serviceAccountId) {
        if (!serviceAccountId) {
            throw new errors_1.PineconeArgumentError('You must pass a non-empty string for `serviceAccountId` in order to delete a service account.');
        }
        return await this._api.deleteServiceAccount({
            xPineconeApiVersion: admin_1.X_PINECONE_API_VERSION,
            serviceAccountId,
        });
    }
}
exports.ServiceAccountsResource = ServiceAccountsResource;
//# sourceMappingURL=serviceAccounts.js.map
"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.ProjectsResource = void 0;
const admin_1 = require("../../pinecone-generated-ts-fetch/admin");
const errors_1 = require("../../errors");
/**
 * Operations for managing Pinecone projects within the organization associated with your service
 * account. Accessed via {@link AdminClient.projects}.
 */
class ProjectsResource {
    _api;
    constructor(api) {
        this._api = api;
    }
    /** Create a new project. */
    async create(options) {
        if (!options || !options.name) {
            throw new errors_1.PineconeArgumentError('You must pass a non-empty string for `name` in order to create a project.');
        }
        return await this._api.createProject({
            xPineconeApiVersion: admin_1.X_PINECONE_API_VERSION,
            createProjectRequest: options,
        });
    }
    /** Get a project's details by ID. */
    async describe(projectId) {
        if (!projectId) {
            throw new errors_1.PineconeArgumentError('You must pass a non-empty string for `projectId` in order to describe a project.');
        }
        return await this._api.fetchProject({
            xPineconeApiVersion: admin_1.X_PINECONE_API_VERSION,
            projectId,
        });
    }
    /** List all projects in the organization. */
    async list() {
        return await this._api.listProjects({
            xPineconeApiVersion: admin_1.X_PINECONE_API_VERSION,
        });
    }
    /** Update an existing project by ID. */
    async update(projectId, options) {
        if (!projectId) {
            throw new errors_1.PineconeArgumentError('You must pass a non-empty string for `projectId` in order to update a project.');
        }
        return await this._api.updateProject({
            xPineconeApiVersion: admin_1.X_PINECONE_API_VERSION,
            projectId,
            updateProjectRequest: options ?? {},
        });
    }
    /**
     * Delete a project by ID. Delete its indexes, assistants, backups, and collections first, or the
     * API will reject the request.
     */
    async delete(projectId) {
        if (!projectId) {
            throw new errors_1.PineconeArgumentError('You must pass a non-empty string for `projectId` in order to delete a project.');
        }
        return await this._api.deleteProject({
            xPineconeApiVersion: admin_1.X_PINECONE_API_VERSION,
            projectId,
        });
    }
}
exports.ProjectsResource = ProjectsResource;
//# sourceMappingURL=projects.js.map
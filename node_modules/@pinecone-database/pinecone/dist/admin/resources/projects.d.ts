import { type CreateProjectRequest, type Project, type ProjectList, type ProjectsApi, type UpdateProjectRequest } from '../../pinecone-generated-ts-fetch/admin';
/**
 * Options for creating a new project (the body of `admin.projects.create`).
 */
export type CreateProjectOptions = CreateProjectRequest;
/**
 * Options for updating an existing project (the body of `admin.projects.update`).
 */
export type UpdateProjectOptions = UpdateProjectRequest;
/**
 * Operations for managing Pinecone projects within the organization associated with your service
 * account. Accessed via {@link AdminClient.projects}.
 */
export declare class ProjectsResource {
    private readonly _api;
    constructor(api: ProjectsApi);
    /** Create a new project. */
    create(options: CreateProjectOptions): Promise<Project>;
    /** Get a project's details by ID. */
    describe(projectId: string): Promise<Project>;
    /** List all projects in the organization. */
    list(): Promise<ProjectList>;
    /** Update an existing project by ID. */
    update(projectId: string, options: UpdateProjectOptions): Promise<Project>;
    /**
     * Delete a project by ID. Delete its indexes, assistants, backups, and collections first, or the
     * API will reject the request.
     */
    delete(projectId: string): Promise<void>;
}

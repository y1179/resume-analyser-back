"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.RoleBindingsResource = void 0;
const admin_1 = require("../../pinecone-generated-ts-fetch/admin");
const errors_1 = require("../../errors");
/**
 * Operations for managing role bindings, which grant roles to principals (users, service accounts,
 * API keys, and invites) at an organization or project scope. Accessed via
 * {@link AdminClient.roleBindings}.
 */
class RoleBindingsResource {
    _api;
    constructor(api) {
        this._api = api;
    }
    /** Create a new role binding. */
    async create(options) {
        if (!options || !options.principalId) {
            throw new errors_1.PineconeArgumentError('You must pass a non-empty string for `principalId` in order to create a role binding.');
        }
        if (!options.principalType) {
            throw new errors_1.PineconeArgumentError('You must pass a non-empty string for `principalType` in order to create a role binding.');
        }
        if (!options.resourceType) {
            throw new errors_1.PineconeArgumentError('You must pass a non-empty string for `resourceType` in order to create a role binding.');
        }
        if (!options.role) {
            throw new errors_1.PineconeArgumentError('You must pass a non-empty string for `role` in order to create a role binding.');
        }
        return await this._api.createRoleBinding({
            xPineconeApiVersion: admin_1.X_PINECONE_API_VERSION,
            createRoleBindingRequest: options,
        });
    }
    /** Get a role binding's details by ID. */
    async describe(roleBindingId) {
        if (!roleBindingId) {
            throw new errors_1.PineconeArgumentError('You must pass a non-empty string for `roleBindingId` in order to describe a role binding.');
        }
        return await this._api.fetchRoleBinding({
            xPineconeApiVersion: admin_1.X_PINECONE_API_VERSION,
            roleBindingId,
        });
    }
    /** List role bindings, optionally filtered by principal, resource, or role. */
    async list(options = {}) {
        return await this._api.listRoleBindings({
            ...options,
            xPineconeApiVersion: admin_1.X_PINECONE_API_VERSION,
        });
    }
    /** Delete a role binding by ID. */
    async delete(roleBindingId) {
        if (!roleBindingId) {
            throw new errors_1.PineconeArgumentError('You must pass a non-empty string for `roleBindingId` in order to delete a role binding.');
        }
        return await this._api.deleteRoleBinding({
            xPineconeApiVersion: admin_1.X_PINECONE_API_VERSION,
            roleBindingId,
        });
    }
}
exports.RoleBindingsResource = RoleBindingsResource;
//# sourceMappingURL=roleBindings.js.map
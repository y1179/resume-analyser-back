"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.OrganizationsResource = void 0;
const admin_1 = require("../../pinecone-generated-ts-fetch/admin");
const errors_1 = require("../../errors");
/**
 * Operations for managing the organizations available to your service account. Accessed via
 * {@link AdminClient.organizations}.
 */
class OrganizationsResource {
    _api;
    constructor(api) {
        this._api = api;
    }
    /** Get an organization's details by ID. */
    async describe(organizationId) {
        if (!organizationId) {
            throw new errors_1.PineconeArgumentError('You must pass a non-empty string for `organizationId` in order to describe an organization.');
        }
        return await this._api.fetchOrganization({
            xPineconeApiVersion: admin_1.X_PINECONE_API_VERSION,
            organizationId,
        });
    }
    /** List all organizations available to the authenticated service account. */
    async list() {
        return await this._api.listOrganizations({
            xPineconeApiVersion: admin_1.X_PINECONE_API_VERSION,
        });
    }
    /** Update an existing organization by ID. */
    async update(organizationId, options) {
        if (!organizationId) {
            throw new errors_1.PineconeArgumentError('You must pass a non-empty string for `organizationId` in order to update an organization.');
        }
        return await this._api.updateOrganization({
            xPineconeApiVersion: admin_1.X_PINECONE_API_VERSION,
            organizationId,
            updateOrganizationRequest: options ?? {},
        });
    }
    /**
     * Delete an organization by ID. All projects within the organization must be deleted first, or
     * the API will reject the request.
     */
    async delete(organizationId) {
        if (!organizationId) {
            throw new errors_1.PineconeArgumentError('You must pass a non-empty string for `organizationId` in order to delete an organization.');
        }
        return await this._api.deleteOrganization({
            xPineconeApiVersion: admin_1.X_PINECONE_API_VERSION,
            organizationId,
        });
    }
}
exports.OrganizationsResource = OrganizationsResource;
//# sourceMappingURL=organizations.js.map
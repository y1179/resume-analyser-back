import { type APIKey, type APIKeysApi, type APIKeyWithSecret, type CreateAPIKeyRequest, type ListApiKeysResponse, type UpdateAPIKeyRequest } from '../../pinecone-generated-ts-fetch/admin';
/**
 * Options for creating a new API key (the body of `admin.apiKeys.create`). Valid `roles` values are
 * `ProjectEditor`, `ProjectViewer`, `ControlPlaneEditor`, `ControlPlaneViewer`, `DataPlaneEditor`,
 * and `DataPlaneViewer`; defaults to `['ProjectEditor']`.
 */
export type CreateApiKeyOptions = CreateAPIKeyRequest;
/**
 * Options for updating an existing API key (the body of `admin.apiKeys.update`). Any field omitted
 * is left unchanged; supplying `roles` replaces the existing set.
 */
export type UpdateApiKeyOptions = UpdateAPIKeyRequest;
/**
 * Operations for managing API keys within a project. Accessed via {@link AdminClient.apiKeys}.
 */
export declare class ApiKeysResource {
    private readonly _api;
    constructor(api: APIKeysApi);
    /**
     * Create a new API key within a project. The returned {@link APIKeyWithSecret} contains the key's
     * secret value, which is returned only once and cannot be retrieved later.
     */
    create(projectId: string, options: CreateApiKeyOptions): Promise<APIKeyWithSecret>;
    /** Get an API key's details by ID. */
    describe(apiKeyId: string): Promise<APIKey>;
    /** List all API keys within a project. */
    list(projectId: string): Promise<ListApiKeysResponse>;
    /** Update an existing API key by ID. */
    update(apiKeyId: string, options: UpdateApiKeyOptions): Promise<APIKey>;
    /** Delete an API key by ID. */
    delete(apiKeyId: string): Promise<void>;
}

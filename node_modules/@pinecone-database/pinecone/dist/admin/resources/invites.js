"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.InvitesResource = void 0;
const admin_1 = require("../../pinecone-generated-ts-fetch/admin");
const errors_1 = require("../../errors");
/**
 * Operations for managing invitations to join the organization. Accessed via
 * {@link AdminClient.invites}.
 */
class InvitesResource {
    _api;
    constructor(api) {
        this._api = api;
    }
    /** Create and send a new invite to join the organization. */
    async create(options) {
        if (!options || !options.email) {
            throw new errors_1.PineconeArgumentError('You must pass a non-empty string for `email` in order to create an invite.');
        }
        if (!options.roleBindings || options.roleBindings.length === 0) {
            throw new errors_1.PineconeArgumentError('You must pass at least one role binding in `roleBindings` in order to create an invite.');
        }
        return await this._api.createInvite({
            xPineconeApiVersion: admin_1.X_PINECONE_API_VERSION,
            createInviteRequest: options,
        });
    }
    /** Get an invite's details by ID. */
    async describe(inviteId) {
        if (!inviteId) {
            throw new errors_1.PineconeArgumentError('You must pass a non-empty string for `inviteId` in order to describe an invite.');
        }
        return await this._api.fetchInvite({
            xPineconeApiVersion: admin_1.X_PINECONE_API_VERSION,
            inviteId,
        });
    }
    /** List invites in the organization. */
    async list(options = {}) {
        return await this._api.listInvites({
            ...options,
            xPineconeApiVersion: admin_1.X_PINECONE_API_VERSION,
        });
    }
    /** Resend an existing invite by ID, extending its expiration. */
    async resend(inviteId) {
        if (!inviteId) {
            throw new errors_1.PineconeArgumentError('You must pass a non-empty string for `inviteId` in order to resend an invite.');
        }
        return await this._api.resendInvite({
            xPineconeApiVersion: admin_1.X_PINECONE_API_VERSION,
            inviteId,
        });
    }
    /** Delete an invite by ID. */
    async delete(inviteId) {
        if (!inviteId) {
            throw new errors_1.PineconeArgumentError('You must pass a non-empty string for `inviteId` in order to delete an invite.');
        }
        return await this._api.deleteInvite({
            xPineconeApiVersion: admin_1.X_PINECONE_API_VERSION,
            inviteId,
        });
    }
}
exports.InvitesResource = InvitesResource;
//# sourceMappingURL=invites.js.map
"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.ApiKeysResource = void 0;
const admin_1 = require("../../pinecone-generated-ts-fetch/admin");
const errors_1 = require("../../errors");
/**
 * Operations for managing API keys within a project. Accessed via {@link AdminClient.apiKeys}.
 */
class ApiKeysResource {
    _api;
    constructor(api) {
        this._api = api;
    }
    /**
     * Create a new API key within a project. The returned {@link APIKeyWithSecret} contains the key's
     * secret value, which is returned only once and cannot be retrieved later.
     */
    async create(projectId, options) {
        if (!projectId) {
            throw new errors_1.PineconeArgumentError('You must pass a non-empty string for `projectId` in order to create an API key.');
        }
        if (!options || !options.name) {
            throw new errors_1.PineconeArgumentError('You must pass a non-empty string for `name` in order to create an API key.');
        }
        return await this._api.createApiKey({
            xPineconeApiVersion: admin_1.X_PINECONE_API_VERSION,
            projectId,
            createAPIKeyRequest: options,
        });
    }
    /** Get an API key's details by ID. */
    async describe(apiKeyId) {
        if (!apiKeyId) {
            throw new errors_1.PineconeArgumentError('You must pass a non-empty string for `apiKeyId` in order to describe an API key.');
        }
        return await this._api.fetchApiKey({
            xPineconeApiVersion: admin_1.X_PINECONE_API_VERSION,
            apiKeyId,
        });
    }
    /** List all API keys within a project. */
    async list(projectId) {
        if (!projectId) {
            throw new errors_1.PineconeArgumentError('You must pass a non-empty string for `projectId` in order to list API keys.');
        }
        return await this._api.listProjectApiKeys({
            xPineconeApiVersion: admin_1.X_PINECONE_API_VERSION,
            projectId,
        });
    }
    /** Update an existing API key by ID. */
    async update(apiKeyId, options) {
        if (!apiKeyId) {
            throw new errors_1.PineconeArgumentError('You must pass a non-empty string for `apiKeyId` in order to update an API key.');
        }
        return await this._api.updateApiKey({
            xPineconeApiVersion: admin_1.X_PINECONE_API_VERSION,
            apiKeyId,
            updateAPIKeyRequest: options ?? {},
        });
    }
    /** Delete an API key by ID. */
    async delete(apiKeyId) {
        if (!apiKeyId) {
            throw new errors_1.PineconeArgumentError('You must pass a non-empty string for `apiKeyId` in order to delete an API key.');
        }
        return await this._api.deleteApiKey({
            xPineconeApiVersion: admin_1.X_PINECONE_API_VERSION,
            apiKeyId,
        });
    }
}
exports.ApiKeysResource = ApiKeysResource;
//# sourceMappingURL=apiKeys.js.map
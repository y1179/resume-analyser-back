"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.listPreviewIndexBackups = listPreviewIndexBackups;
const db_control_1 = require("../../pinecone-generated-ts-fetch-alpha/db_control");
const errors_1 = require("../../errors");
const handling_1 = require("../../errors/handling");
/**
 * Lists all backups for an index using the 2026-01.alpha API.
 *
 * @see [Backups](https://docs.pinecone.io/guides/indexes/backups)
 * @alpha
 */
async function listPreviewIndexBackups(api, indexName, options = {}) {
    if (!indexName) {
        throw new errors_1.PineconeArgumentError('You must pass a non-empty string for `indexName` in order to list backups.');
    }
    try {
        return await api.listIndexBackups({
            indexName,
            limit: options.limit,
            paginationToken: options.paginationToken,
            xPineconeApiVersion: db_control_1.X_PINECONE_API_VERSION,
        });
    }
    catch (e) {
        throw await (0, handling_1.handleApiError)(e, async (_, rawMessageText) => `Error listing backups for index ${indexName}: ${rawMessageText}`);
    }
}
//# sourceMappingURL=listIndexBackups.js.map
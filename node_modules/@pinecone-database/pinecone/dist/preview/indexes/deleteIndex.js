"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.deletePreviewIndex = deletePreviewIndex;
const db_control_1 = require("../../pinecone-generated-ts-fetch-alpha/db_control");
const errors_1 = require("../../errors");
const handling_1 = require("../../errors/handling");
/**
 * Deletes an index by name.
 *
 * Deletion is asynchronous; the index may still be terminating after
 * this call returns. Deletion protection must be disabled on the index
 * before calling this method.
 *
 * @alpha
 */
async function deletePreviewIndex(api, name) {
    if (!name) {
        throw new errors_1.PineconeArgumentError('You must pass a non-empty string for `name` in order to delete an index.');
    }
    try {
        await api.deleteIndex({
            indexName: name,
            xPineconeApiVersion: db_control_1.X_PINECONE_API_VERSION,
        });
    }
    catch (e) {
        throw await (0, handling_1.handleApiError)(e, async (_, rawMessageText) => `Error deleting preview index ${name}: ${rawMessageText}`);
    }
}
//# sourceMappingURL=deleteIndex.js.map
"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.searchPreviewDocuments = void 0;
const db_data_1 = require("../../pinecone-generated-ts-fetch-alpha/db_data");
const errors_1 = require("../../errors");
const handling_1 = require("../../errors/handling");
const searchPreviewDocuments = async (api, namespace, options) => {
    if (!options.scoreBy || options.scoreBy.length === 0) {
        throw new errors_1.PineconeArgumentError('You must pass a non-empty `scoreBy` array to searchDocuments.');
    }
    if (options.topK == null || options.topK < 1) {
        throw new errors_1.PineconeArgumentError('`topK` must be a positive integer of at least 1.');
    }
    try {
        return await api.searchDocuments({
            namespace,
            searchDocumentsRequest: options,
            xPineconeApiVersion: db_data_1.X_PINECONE_API_VERSION,
        });
    }
    catch (e) {
        throw await (0, handling_1.handleApiError)(e, async (_, rawMessageText) => `Error searching documents in namespace ${namespace}: ${rawMessageText}`);
    }
};
exports.searchPreviewDocuments = searchPreviewDocuments;
//# sourceMappingURL=searchDocuments.js.map
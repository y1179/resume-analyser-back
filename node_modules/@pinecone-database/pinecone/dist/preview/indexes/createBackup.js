"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.createPreviewBackup = createPreviewBackup;
const db_control_1 = require("../../pinecone-generated-ts-fetch-alpha/db_control");
const errors_1 = require("../../errors");
const handling_1 = require("../../errors/handling");
/**
 * Creates a backup of an index using the 2026-01.alpha API.
 *
 * @see [Backups](https://docs.pinecone.io/guides/indexes/backups)
 * @alpha
 */
async function createPreviewBackup(api, indexName, options = {}) {
    if (!indexName) {
        throw new errors_1.PineconeArgumentError('You must pass a non-empty string for `indexName` in order to create a backup.');
    }
    try {
        return await api.createBackup({
            indexName,
            createBackupRequest: options,
            xPineconeApiVersion: db_control_1.X_PINECONE_API_VERSION,
        });
    }
    catch (e) {
        throw await (0, handling_1.handleApiError)(e, async (_, rawMessageText) => `Error creating backup for index ${indexName}: ${rawMessageText}`);
    }
}
//# sourceMappingURL=createBackup.js.map
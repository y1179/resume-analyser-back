"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.deletePreviewDocuments = void 0;
const db_data_1 = require("../../pinecone-generated-ts-fetch-alpha/db_data");
const errors_1 = require("../../errors");
const handling_1 = require("../../errors/handling");
const deletePreviewDocuments = async (api, namespace, options) => {
    if (!options.ids && !options.deleteAll) {
        throw new errors_1.PineconeArgumentError('You must specify either `ids` or `deleteAll` to deleteDocuments.');
    }
    if (options.ids && options.deleteAll) {
        throw new errors_1.PineconeArgumentError('`ids` and `deleteAll` are mutually exclusive in deleteDocuments.');
    }
    if (options.ids && options.ids.length === 0) {
        throw new errors_1.PineconeArgumentError('`ids` must contain at least one document ID in deleteDocuments.');
    }
    try {
        await api.deleteDocuments({
            namespace,
            deleteDocumentsRequest: options,
            xPineconeApiVersion: db_data_1.X_PINECONE_API_VERSION,
        });
    }
    catch (e) {
        throw await (0, handling_1.handleApiError)(e, async (_, msg) => `Error deleting documents from namespace ${namespace}: ${msg}`);
    }
};
exports.deletePreviewDocuments = deletePreviewDocuments;
//# sourceMappingURL=deleteDocuments.js.map
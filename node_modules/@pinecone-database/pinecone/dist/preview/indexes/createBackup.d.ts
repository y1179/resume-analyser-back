import type { ManageIndexesApi, BackupModel } from '../../pinecone-generated-ts-fetch-alpha/db_control';
/**
 * Options for creating a backup of an index.
 *
 * @alpha
 */
export interface PreviewCreateBackupOptions {
    /** Optional user-defined name for the backup. */
    name?: string;
    /** Optional description providing context for the backup. */
    description?: string;
}
/**
 * Creates a backup of an index using the 2026-01.alpha API.
 *
 * @see [Backups](https://docs.pinecone.io/guides/indexes/backups)
 * @alpha
 */
export declare function createPreviewBackup(api: ManageIndexesApi, indexName: string, options?: PreviewCreateBackupOptions): Promise<BackupModel>;

"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.createPreviewCollection = createPreviewCollection;
const db_control_1 = require("../../pinecone-generated-ts-fetch-alpha/db_control");
const errors_1 = require("../../errors");
const handling_1 = require("../../errors/handling");
/**
 * Creates a collection from a pod-based index using the 2026-01.alpha API.
 *
 * Collections snapshot the current state of a pod-based index. Serverless indexes
 * do not support collections.
 *
 * @param api - The alpha manage-indexes API client.
 * @param options - Collection name and source index name.
 * @see [Collections](https://docs.pinecone.io/guides/indexes/collections/understanding-collections)
 * @alpha
 */
async function createPreviewCollection(api, options) {
    if (!options.name) {
        throw new errors_1.PineconeArgumentError('You must pass a non-empty string for `name` in order to create a collection.');
    }
    if (!options.source) {
        throw new errors_1.PineconeArgumentError('You must pass a non-empty string for `source` in order to create a collection.');
    }
    try {
        return await api.createCollection({
            createCollectionRequest: options,
            xPineconeApiVersion: db_control_1.X_PINECONE_API_VERSION,
        });
    }
    catch (e) {
        throw await (0, handling_1.handleApiError)(e, async (_, rawMessageText) => `Error creating preview collection ${options.name}: ${rawMessageText}`);
    }
}
//# sourceMappingURL=createCollection.js.map
"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.configurePreviewIndex = configurePreviewIndex;
const db_control_1 = require("../../pinecone-generated-ts-fetch-alpha/db_control");
const errors_1 = require("../../errors");
const handling_1 = require("../../errors/handling");
/**
 * Configures an index using the 2026-01.alpha API.
 *
 * Only the fields present in `options` are updated; omit a field to leave it unchanged.
 *
 * @alpha
 */
async function configurePreviewIndex(api, name, options) {
    if (!name) {
        throw new errors_1.PineconeArgumentError('You must pass a non-empty string for `name` in order to configure an index.');
    }
    try {
        return await api.configureIndex({
            indexName: name,
            configureIndexRequest: options,
            xPineconeApiVersion: db_control_1.X_PINECONE_API_VERSION,
        });
    }
    catch (e) {
        throw await (0, handling_1.handleApiError)(e, async (_, rawMessageText) => `Error configuring preview index ${name}: ${rawMessageText}`);
    }
}
//# sourceMappingURL=configureIndex.js.map
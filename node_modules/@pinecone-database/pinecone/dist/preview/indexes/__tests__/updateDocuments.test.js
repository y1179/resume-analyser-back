"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
Object.defineProperty(exports, "__esModule", { value: true });
const updateDocuments_1 = require("../updateDocuments");
const errors_1 = require("../../../errors");
const buildMockApi = (overrides = {}) => ({
    updateDocuments: jest.fn().mockResolvedValue({}),
    ...overrides,
});
const validOptions = {
    documents: [
        { _id: 'doc-1', chunk_text: 'Updated content' },
        { _id: 'doc-2', _remove_fields: ['category'] },
    ],
};
describe('updatePreviewDocuments', () => {
    describe('validation', () => {
        test('throws PineconeArgumentError when documents is an empty array', async () => {
            await expect((0, updateDocuments_1.updatePreviewDocuments)(buildMockApi(), 'test-ns', { documents: [] })).rejects.toThrow(errors_1.PineconeArgumentError);
        });
        test('error message references `documents` when the array is empty', async () => {
            await expect((0, updateDocuments_1.updatePreviewDocuments)(buildMockApi(), 'test-ns', { documents: [] })).rejects.toThrow('`documents`');
        });
        test('throws PineconeArgumentError when documents is missing', async () => {
            await expect((0, updateDocuments_1.updatePreviewDocuments)(buildMockApi(), 'test-ns', {})).rejects.toThrow(errors_1.PineconeArgumentError);
        });
    });
    describe('success', () => {
        test('resolves to void', async () => {
            const result = await (0, updateDocuments_1.updatePreviewDocuments)(buildMockApi(), 'test-ns', validOptions);
            expect(result).toBeUndefined();
        });
        test('passes namespace to the API call', async () => {
            const api = buildMockApi();
            await (0, updateDocuments_1.updatePreviewDocuments)(api, 'my-namespace', validOptions);
            expect(api.updateDocuments).toHaveBeenCalledWith(expect.objectContaining({ namespace: 'my-namespace' }));
        });
        test('passes documents (including _remove_fields) to the API call', async () => {
            const api = buildMockApi();
            await (0, updateDocuments_1.updatePreviewDocuments)(api, 'test-ns', validOptions);
            expect(api.updateDocuments).toHaveBeenCalledWith(expect.objectContaining({
                updateDocumentsRequest: expect.objectContaining({
                    documents: validOptions.documents,
                }),
            }));
        });
        test('passes xPineconeApiVersion containing 2026 in every call', async () => {
            const api = buildMockApi();
            await (0, updateDocuments_1.updatePreviewDocuments)(api, 'test-ns', validOptions);
            expect(api.updateDocuments).toHaveBeenCalledWith(expect.objectContaining({
                xPineconeApiVersion: expect.stringContaining('2026'),
            }));
        });
    });
    describe('error handling', () => {
        test('converts ResponseError (404) to a typed Pinecone error via handleApiError', async () => {
            const { ResponseError } = await Promise.resolve().then(() => __importStar(require('../../../pinecone-generated-ts-fetch-alpha/db_data')));
            const api = buildMockApi({
                updateDocuments: jest.fn().mockRejectedValue(new ResponseError(new Response('{"code": 5, "message": "Index not found"}', {
                    status: 404,
                }), 'Not Found')),
            });
            await expect((0, updateDocuments_1.updatePreviewDocuments)(api, 'test-ns', validOptions)).rejects.toBeDefined();
        });
    });
});
//# sourceMappingURL=updateDocuments.test.js.map
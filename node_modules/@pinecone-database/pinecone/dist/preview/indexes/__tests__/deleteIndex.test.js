"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
Object.defineProperty(exports, "__esModule", { value: true });
const deleteIndex_1 = require("../deleteIndex");
const errors_1 = require("../../../errors");
const buildMockApi = (overrides = {}) => ({
    deleteIndex: jest.fn().mockResolvedValue(undefined),
    ...overrides,
});
describe('deletePreviewIndex', () => {
    describe('validation', () => {
        test('throws PineconeArgumentError when name is empty string', async () => {
            await expect((0, deleteIndex_1.deletePreviewIndex)(buildMockApi(), '')).rejects.toThrow(errors_1.PineconeArgumentError);
        });
        test('error message references the `name` field', async () => {
            await expect((0, deleteIndex_1.deletePreviewIndex)(buildMockApi(), '')).rejects.toThrow('`name`');
        });
    });
    describe('success', () => {
        test('calls api.deleteIndex with correct indexName', async () => {
            const api = buildMockApi();
            await (0, deleteIndex_1.deletePreviewIndex)(api, 'my-index');
            expect(api.deleteIndex).toHaveBeenCalledWith(expect.objectContaining({ indexName: 'my-index' }));
        });
        test('returns undefined (void) on success', async () => {
            const result = await (0, deleteIndex_1.deletePreviewIndex)(buildMockApi(), 'my-index');
            expect(result).toBeUndefined();
        });
        test('uses alpha API version header', async () => {
            const api = buildMockApi();
            await (0, deleteIndex_1.deletePreviewIndex)(api, 'my-index');
            expect(api.deleteIndex).toHaveBeenCalledWith(expect.objectContaining({
                xPineconeApiVersion: expect.stringContaining('2026'),
            }));
        });
    });
    describe('error handling', () => {
        test('rethrows error from handleApiError', async () => {
            const api = buildMockApi({
                deleteIndex: jest.fn().mockRejectedValue(new Error('Network error')),
            });
            await expect((0, deleteIndex_1.deletePreviewIndex)(api, 'my-index')).rejects.toBeDefined();
        });
        test('converts ResponseError to a typed Pinecone error', async () => {
            const { ResponseError } = await Promise.resolve().then(() => __importStar(require('../../../pinecone-generated-ts-fetch-alpha/db_control')));
            const api = buildMockApi({
                deleteIndex: jest
                    .fn()
                    .mockRejectedValue(new ResponseError(new Response('{"error": "Index not found"}', { status: 404 }), 'Not Found')),
            });
            await expect((0, deleteIndex_1.deletePreviewIndex)(api, 'missing-index')).rejects.toBeDefined();
        });
        test('throws on deletion-protection 403', async () => {
            const { ResponseError } = await Promise.resolve().then(() => __importStar(require('../../../pinecone-generated-ts-fetch-alpha/db_control')));
            const api = buildMockApi({
                deleteIndex: jest
                    .fn()
                    .mockRejectedValue(new ResponseError(new Response('{"error": "Deletion protection is enabled for this index."}', { status: 403 }), 'Forbidden')),
            });
            await expect((0, deleteIndex_1.deletePreviewIndex)(api, 'protected-index')).rejects.toBeDefined();
        });
    });
});
//# sourceMappingURL=deleteIndex.test.js.map
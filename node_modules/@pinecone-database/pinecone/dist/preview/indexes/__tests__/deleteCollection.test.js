"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
Object.defineProperty(exports, "__esModule", { value: true });
const deleteCollection_1 = require("../deleteCollection");
const errors_1 = require("../../../errors");
const buildMockApi = (overrides = {}) => ({
    deleteCollection: jest.fn().mockResolvedValue(undefined),
    ...overrides,
});
describe('deletePreviewCollection', () => {
    describe('validation', () => {
        test('throws PineconeArgumentError when collectionName is empty', async () => {
            await expect((0, deleteCollection_1.deletePreviewCollection)(buildMockApi(), '')).rejects.toThrow(errors_1.PineconeArgumentError);
            await expect((0, deleteCollection_1.deletePreviewCollection)(buildMockApi(), '')).rejects.toThrow('`collectionName`');
        });
    });
    describe('success', () => {
        test('resolves without a value on success', async () => {
            const result = await (0, deleteCollection_1.deletePreviewCollection)(buildMockApi(), 'my-collection');
            expect(result).toBeUndefined();
        });
        test('calls api.deleteCollection with the correct collectionName', async () => {
            const api = buildMockApi();
            await (0, deleteCollection_1.deletePreviewCollection)(api, 'my-collection');
            expect(api.deleteCollection).toHaveBeenCalledWith(expect.objectContaining({ collectionName: 'my-collection' }));
        });
        test('calls api.deleteCollection with alpha API version header', async () => {
            const api = buildMockApi();
            await (0, deleteCollection_1.deletePreviewCollection)(api, 'my-collection');
            expect(api.deleteCollection).toHaveBeenCalledWith(expect.objectContaining({
                xPineconeApiVersion: expect.stringContaining('2026'),
            }));
        });
    });
    describe('error handling', () => {
        test('converts 404 ResponseError to a thrown error', async () => {
            const { ResponseError } = await Promise.resolve().then(() => __importStar(require('../../../pinecone-generated-ts-fetch-alpha/db_control')));
            const api = buildMockApi({
                deleteCollection: jest
                    .fn()
                    .mockRejectedValue(new ResponseError(new Response('Not Found', { status: 404 }), 'Not Found')),
            });
            await expect((0, deleteCollection_1.deletePreviewCollection)(api, 'missing-collection')).rejects.toBeDefined();
        });
    });
});
//# sourceMappingURL=deleteCollection.test.js.map
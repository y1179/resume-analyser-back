import type { ManageIndexesApi, IndexModel, ConfigureIndexRequest } from '../../pinecone-generated-ts-fetch-alpha/db_control';
/**
 * Options for configuring an alpha index using the 2026-01.alpha API.
 *
 * All fields are optional — send only those you want to change.
 *
 * @alpha
 */
export type PreviewConfigureIndexOptions = ConfigureIndexRequest;
export type { PatchIndexDeploymentRequest as PreviewPatchIndexDeploymentRequest, PatchIndexSchema as PreviewPatchIndexSchema, PatchSemanticTextField as PreviewPatchSemanticTextField, } from '../../pinecone-generated-ts-fetch-alpha/db_control';
/**
 * Configures an index using the 2026-01.alpha API.
 *
 * Only the fields present in `options` are updated; omit a field to leave it unchanged.
 *
 * @alpha
 */
export declare function configurePreviewIndex(api: ManageIndexesApi, name: string, options: PreviewConfigureIndexOptions): Promise<IndexModel>;

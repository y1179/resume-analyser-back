"use strict";
/**
 * Shared tokenizer utility for all Voyage models (local and API).
 * Loads tokenizers from HuggingFace Hub via @huggingface/transformers.
 */
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.getTransformers = getTransformers;
exports.getTokenizerForModel = getTokenizerForModel;
exports.tokenizeTexts = tokenizeTexts;
const tokenizerCache = new Map();
let transformersModule = null;
function getTransformers() {
    return __awaiter(this, void 0, void 0, function* () {
        if (!transformersModule) {
            try {
                transformersModule = yield Promise.resolve().then(() => __importStar(require("@huggingface/transformers")));
            }
            catch (_a) {
                throw new Error("Tokenizer requires @huggingface/transformers. " +
                    "Install: npm i @huggingface/transformers onnxruntime-node");
            }
        }
        return transformersModule;
    });
}
/** Load tokenizer for any Voyage model (cached). */
function getTokenizerForModel(model) {
    return __awaiter(this, void 0, void 0, function* () {
        let tokenizer = tokenizerCache.get(model);
        if (!tokenizer) {
            const { AutoTokenizer } = yield getTransformers();
            tokenizer = yield AutoTokenizer.from_pretrained(`voyageai/${model}`);
            tokenizerCache.set(model, tokenizer);
        }
        return tokenizer;
    });
}
/** Tokenize texts using the model's tokenizer. */
function tokenizeTexts(model, texts) {
    return __awaiter(this, void 0, void 0, function* () {
        if (texts.length === 0) {
            return [];
        }
        const tokenizer = yield getTokenizerForModel(model);
        const results = [];
        for (const text of texts) {
            const encoded = tokenizer(text);
            const ids = Array.from(encoded.input_ids.data, (v) => Number(v));
            const tokens = ids.map((id) => { var _a; return (_a = tokenizer.model.vocab[id]) !== null && _a !== void 0 ? _a : `<unk:${id}>`; });
            results.push({ tokens, ids });
        }
        return results;
    });
}
